#!/usr/bin/env python3
"""Sustained native memory throughput; see README.md for measurement semantics."""
import sys

# Omarchy watches the plugin directory: bytecode writes would reload the panel
# and cancel its first run after installation or a Python source update.
sys.dont_write_bytecode = True

from benchmark import main

if __name__ == "__main__":
    main()
